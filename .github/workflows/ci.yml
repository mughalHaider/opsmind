name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a test
        run: echo "Hello from OpsMind CI"
      - name: Force failure (temporary, for testing)
        run: exit 1